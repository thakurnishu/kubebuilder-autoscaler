---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.16.4
  name: scaledobjects.scaler.nishantsingh.sh
spec:
  group: scaler.nishantsingh.sh
  names:
    kind: ScaledObject
    listKind: ScaledObjectList
    plural: scaledobjects
    singular: scaledobject
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: ScaledObject is the Schema for the scaledobjects API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: ScaledObjectSpec defines the desired state of ScaledObject.
            properties:
              deploymentName:
                description: Name of Deployment Need to Watch for scaling operation
                type: string
              namespace:
                description: Namespace of Deployment Need to Watch for scaling operation
                type: string
            required:
            - deploymentName
            - namespace
            type: object
          status:
            description: ScaledObjectStatus defines the observed state of ScaledObject.
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
